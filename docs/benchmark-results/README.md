# Benchmark Result Snapshots

This directory stores machine-specific benchmark artifacts.

Current status on the maintainer machine:

- CPU: `13th Gen Intel(R) Core(TM) i9-13900H`
- logical CPU count observed by the benchmark: `20`
- OS: `Linux 6.19.6-200.fc43.x86_64 x86_64`
- compiler: `GNU 15.2.1`

Available artifact:

- [2026-03-21-i9-13900h-smoke-light-short.md](2026-03-21-i9-13900h-smoke-light-short.md)
- [2026-03-21-i9-13900h-smoke-light-short.json](2026-03-21-i9-13900h-smoke-light-short.json)

Important limitation:

- requested long-duration `full` run still fails on this machine (`http_get_static/light` timeout in the latest run)
- short-duration `full` smoke with all pressure levels also fails (`http_post_echo_1k/saturated` timeout in the latest run)
- therefore this directory keeps a clearly labeled **light-pressure smoke snapshot** as the latest comparable baseline
- smoke parameters are shortened (`warmup=100ms`, `duration=200ms`, `cooldown=50ms`, `repetitions=1`) and constrained to `--pressure light`

Once the library issue is fixed, this directory should be updated with a full multi-round snapshot generated by:

```bash
./build-bench/benchmarks/bsrvcore_http_benchmark \
  --profile full \
  --output-json docs/benchmark-results/<date>-<machine>-full.json
```
